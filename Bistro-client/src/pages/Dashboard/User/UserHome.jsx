import useTitle from "../../../hooks/useTitle";

const UserHome = () => {
      useTitle("Dashboard");
      return (
            <>
                  user home
            </>
      )
}

export default UserHome